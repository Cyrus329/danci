B163 基于B162，词库/音标/快速复盘功能保留。
本轮修复：
1. 主本地存档无损去重打包；旧键保留并支持读取。容量不足时仅在大容量数据库提交成功、旧原文已逐字存档后释放旧本地副本；原文保留在b163-preserved:legacy-local。
2. 大容量数据库启动读取按词逐模式的实际操作时间合并，不整份替换正在使用的学习记录。
3. 不再因新快照数量少就自动选旧快照。最新“不会”重置也必须有效。
4. 读库完成之前不写入大容量数据库；首次迁移前保留主快照、上次快照、本地原文。
5. 本地保存失败明确提示；不再为保存失败直接截掉内存中的历史。

找回记录：将包解压覆盖当前正在使用的B162文件夹，保持index.html完整路径不变。
在新标签页打开同一个index.html，将地址末尾改为#storage-rescue。
此入口不加载app.js，不执行学习/云同步，不写入存档；点击下载本机存档线索后发回核对。
它导出该路径下的本地数据及同名IndexedDB全部快照，不能直接作为普通备份导入。
不要清空浏览器数据；不要再次导入更旧备份。

验证：真实8121词备份转为原compact格式约2752344字符，无损打包后1996303字符；5轮编码解码逐字段一致。
较新主进度、较新重置分别胜过旧操作测试通过。新版本地快照优先于旧版本保存数据测试通过。
这些是代码层测试，尚未访问用户电脑的浏览器存档，不能宣称截图中丢失的记录已经恢复。
系统空间不足、旧版标签页继续运行等情况仍需实际核对。
