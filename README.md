# B151 · 移除内置旧进度，真正清空本机存档
基于B150。保留8013词、精简配图、手机版美化、发音与四级翻译强制拼写。

修复：
- 移除包内B068主学习恢复快照及固定时长、打卡基线。
- 移除B077全词练习恢复快照。
- 取消本地固定复习次数和跨站自动恢复账本。
- Peppa新建存档全部未学习；已有实际学习记录不会在升级时清空。
- 清空页面在原index.html路径下运行，先停止加载学习脚本，清理localStorage、sessionStorage以及IndexedDB快照，避免旧数据重新写回。
- 清空会断开本机云连接，不删除云端存档。用户手动导入或加载旧云存档仍会恢复进度。

如何从零开始：
1. 导出需要保留的原备份，关闭其他打开的单词页面。
2. 上传本包解压后的文件；更新后确认页面版本B151。
3. 更多操作 → 清空本机全部学习进度 → 确认清空。
4. 点击返回主页，从零开始。不要再导入此前的旧进度备份。

B150-to-B151补丁包仅含需要覆盖的文件，不含完整词库和图片，请覆盖在B150上。
上传覆盖不会自动删除服务器上的旧快照文件，但B151已不加载这些文件。可另行删除recovered-progress-b068.js、browse-user-save-b077.js、apply-browse-user-save-b077.js。
现有进度不会仅因升级被清空。清空按钮是用户主动操作，不是导入合并。

验证：35项针对性测试通过；Peppa231条默认全部未学习；316项缓存资源存在。
词库与来源内容逐字节一致；强制拼写、音频核心逻辑和手机版美化保留。
未完成浏览器/手机实机渲染验证。
