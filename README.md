# v70 B031 七模块原位静态版

- 七个大模块入口恢复普通页面位置。
- 页面下滑后模块栏自然离开屏幕，不吸顶、不悬浮、不跟随。
- 词库、分组、进度、备份与云同步结构保持不变。

# v70 B030 七模块顶部吸附修正版

基于 v70 B029“词汇继续导入版”继续更新。

## 本次修复

- 七个大模块入口仍在页面原位置正常显示。
- 页面向下滚过模块入口后，整排模块只吸附到浏览器页面最上方（`top: 0`）。
- 删除 B028/B029 使用的“首次出现位置锚点”，不会再悬浮在页面中部或下方遮挡内容。
- 返回页面顶部后，模块入口自动回到原来的文档位置。
- 全词浏览内部词表仍可独立滚动，搜索、筛选、字母栏与分页功能保留。

## 数据与功能保持不变

- 全库仍为 4745 条。
- 短语练习 20、四级 28、蓝色森林 34 的导入数据保持不变。
- 全词浏览默认按“我的原始顺序”。
- 快速复习、认识度/拼写度、一屏一词四选一、看中文拼英文继续共用同一份进度。
- 手机双存档、JSON 备份和云同步结构未改变。

解压后可直接覆盖上传到 GitHub 仓库根目录。首次打开可在网址末尾临时添加 `?v=b030` 强制刷新缓存。
